> 极简启动：日常双击 `start_booking.bat`，不会同步环境。仅首次安装/环境修复时运行 `setup_environment.bat`。

> 2026-09-17：入口已改为每日快捷模式。主菜单1=今天12点，2=今天立即，3=每日时段，4=可选优先级，5=刷新凭据，6=验证。下方旧编号均指主菜单8中的高级工具。完整最新操作见 USAGE.md 开头。

# 本机登录凭据接入指南

## 状态

捕获和真实提交已做过本机验证，但不代表每次登录仍有效。完整字段与操作说明见USAGE.md，或在统一入口输入10。
统一入口使用本项目 uv 虚拟环境，不修改原仓库、不安装全局模块、不自动安装证书。捕获和验证菜单不创建订单；预约菜单需另行输入 1（确认预约） 确认。

## 操作

1. 在之前手动运行 mitmdump 的窗口按 Ctrl+C，释放 8080。保留 Clash 运行及 7897 监听。
2. 仅捕获本人的账号。关闭无关网页和应用，不在捕获期间进行其他敏感操作。
3. 若截图仍提示 CA 未信任，先检查自己的本机证书（不要下载他人的 CA）：

```powershell
$cert = Join-Path $env:USERPROFILE '.mitmproxy\mitmproxy-ca-cert.cer'
Test-Path $cert
certutil -dump $cert
```

确认来自本机 mitmproxy 后，以下命令会添加当前用户根信任，只执行一次：

```powershell
certutil -user -addstore Root $cert
```

记下证书指纹。以后不再使用时，通过 certmgr.msc 的“受信任的根证书颁发机构”移除这一张确切的证书，不要批量删除根证书。

4. 重启企业微信/承载小程序的客户端，由本人正常登录。
5. 双击同目录 start_booking.bat（start_session.bat 也转入同一入口），选择 1，输入 1（开始捕获）。
6. 手动进入 企业微信 → 工作台 → 南邮小程序 → 体育场馆预约 → 羽毛球场次页面。不要提交预约。
7. 程序最多等待五分钟；捕获成功、超时、Ctrl+C 都会尝试恢复原系统代理。若强制关窗口/断电，重新打开选择 8 恢复。恢复会覆盖抓包期间对这几个代理字段的其他修改。
8. 选择 2 检查本地凭据并通过 Clash 发送一次只读类型查询验证服务端是否接受凭据；不显示原始响应。菜单 3 是目标配置，不再是凭据验证。

## 数据位置

private/session.json 保存 token、expires_at、captured_at。目录设置当前 Windows 用户和 SYSTEM 权限，仍是明文，不是密码保险箱。不要复制、上传、截图或提交该目录；.gitignore 已排除它。
private/proxy-backup.json 仅在代理尚待恢复时存在。
不保存完整流量、请求体、登录响应或学号。凭据直接进入开发项目，无须从原仓库搬运。

## 接入所需字段

- 本地保留：token（请求头）、expires_at、captured_at。
- 场次解析：localDate、timeFields、startTime、endTime、stadiumInfos、id、name、status。已通过本机场次查询验证，未来结构变化仍需重新核验。
- 后续预约需要确认：订单号、订单状态、支付截止时间、查询订单接口与分页。不能根据旧仓库猜测它们。
- 参考仓库提交还使用本人学号、timestamp、fingerprint，真实提交适配已实现，这些信息留在本机，不需要发出。

## 排障

- 8080 占用：先停旧代理，不自动结束陌生进程。
- 代理被 Clash 改回：保持 Clash 进程及 7897，但暂停系统代理自动接管；不要让两个程序争写系统代理。
- CA 信任后仍失败：客户端可能不遵循系统代理或不接受该 CA，不绕过证书绑定、不关闭服务端 TLS 校验。
- 502：只说明有网关失败，不能证明请求已到学校；先检查 Clash 对南邮域名的 DIRECT 路由及校园网/VPN需求。
- 捕获后本地检查成功但在线验证失败：凭据可能失效，或学校网络/证书/接口有变化；不尝试创建订单。
- 用来解密的域名限制只有 wechat.njupt.edu.cn:443；插件仅读取其场馆查询请求头，不读取其他登录流量。
