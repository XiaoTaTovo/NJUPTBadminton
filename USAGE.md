> 2026-09-18 后续策略更新：明确 `success:false`、无订单生成迹象且未触发认证/限流/账户等停止条件时，即使错误码未识别（如7070）也按候选优先级继续。定时使用提前准备的候选ID，不为每次拒绝插入GET。同一场次本轮只尝试一次；成功后不换场。超时/响应不明仍停止。下方复盘中“7070停止”描述的是更新前的历史行为。

# 每日快捷模式（2026-09-17 更新）

## 每天只需这样操作

1. 双击项目中的 `start_booking.bat`，等待“每日快捷预约”出现，不必连点。
2. 首次看清终端显示的**打球时段和需要几场**。主菜单3可修改并长期保存。
3. 凭据过期时：主菜单5，按捕获提示打开企业微信→场馆小程序；捕获后主菜单6只读验证。不把令牌粘贴到聊天或Git。
4. 中午前选择1（或回车），核对摘要后输入1确认真实预约；程序等待当天12点。
5. 中午已过，1不会补跑。今天仍需预约，明确选择2并再次输入1确认。
6. 成功通知后去小程序核对并自行支付。不会付款、取消或保证有场；期限以小程序为准。

日期每天取北京时间今天；打球时段与场数跨天保留；每次运行仍需确认。不是系统后台任务，关闭窗口或电脑休眠都会影响定时。多个时段允许部分成功，不保证同一场连续时段。

## 快捷主菜单字段

| 输入 | 含义 |
|---|---|
| 1/回车 | 今天12点定时预约；再次确认才执行；过点不补跑 |
| 2 | 今天立即预约；使用当前可用快照；再次确认才执行 |
| 3 | 修改每日打球时段与数量，保存但不预约 |
| 4 | 可选优先级入口；平时不需要进入 |
| 5 | 捕获/刷新本人凭据；临时代理，经Clash出口；结束恢复 |
| 6 | 只读验证登录；不预约 |
| 7 | 最近本地结果，不是实时订单查询 |
| 8 | 高级工具：指定日期/校区/候选、测速、代理恢复等；0返回 |
| 9 | 本帮助 |
| 0 | 退出，不取消任何订单 |

### 每日时段（菜单3）

- “几个时段”：1–6，不同打球时间填不同组；这是组数，不是场地数。
- “开始-结束”：如 `18:00-19:00`，不是列表序号，也不是启动时刻。
- “需要几场”：回车1；2表示这个时段要两块不同场。所有组数量总和不超过6。
- 两个时段各一场：组数2，第1组 `18:00-19:00` 数量1，第2组 `19:00-20:00` 数量1。
- 同时段两场：组数1，`19:00-20:00` 数量2，不建重复组。
- 首次从旧计划迁移打球时段，相同时间组合并数量，不继承过期日期或狭窄候选。已有每日设置则不再次迁移。

### 默认优先级（菜单4可选修改）

**新馆3楼6号 → 3楼其他双打1–8号 → 3楼单打9–12号 → 体育馆1楼任意 → 训练馆2楼任意。**

从接口返回的当天、指定时段自动纳入这四档全部已识别候选；无需每天手选场地。不会纳入三牌楼、无法识别的场馆或配置外的时间。双/单分类是你的偏好定义。默认同档除6号外按场号排序。

自定义时填四档顺序 `1,2,3,4`（各出现一次），再填3楼双打首选场号1–8；首选只在双打档内部生效。选恢复默认即可回到6号优先。

## 定时时序及限制

- 提前检查凭据，用Windows `w32tm /stripchart`只读测量NTP偏差（不修改系统时间）。至少2个有效样本、离散不超过250ms、绝对偏差不超过60秒；否则停止定时。NTP不可达时明确失败，不悄悄回退到本机时钟。
- 校准时间用单调时钟推进，并以HTTP Date做秒级一致性旁证，二者明显冲突则停止。样本离散不等于绝对误差上界，不能保证毫秒精度。早启动时在临近准备窗口重新校准。
- 等待到校准后的11:59左右，查询**当天**真实场次ID，选择首选并写入提交意图。
- 11:59:40左右做一次只读会话预热。预热失败就停止。
- 12:00等待门返回后，首笔直接进入POST，不夹场次GET或日志落盘。首笔成功后，其他已配置目标使用提前准备的ID继续串行提交，省去中间GET；仍遵守请求间隔、目标数量及防重复保护。明确失败时直接尝试下一预备候选；缓存候选耗尽后才重新查询。
- 开放前的不可用标记可能是尚未开放，也可能已占用。定时首笔允许尝试服务器返回的目标ID；不能保证其必然可提交。不猜ID、不复用昨天ID、不提前POST。
- 若当天目标ID尚未返回，或准备完成距12点不足3秒，就明确停止；不会声称已经准备好。
- 仍保持请求间隔至少1秒、串行执行、最多60轮/组最多30次尝试、提交阶段最多60秒（在途请求可能延长）。限流/结果未知停止，不换身份或绕过限制。
- 日志同时记录本机请求时间与校准后参考时间。参考时间不是业务服务器承诺的放场时间，也不是服务器收包/入队时间；Windows调度、校时误差、网络和服务端竞争仍影响结果。**不承诺12:00:00.005到达服务端**，HTTP Date秒级精度也不能支持这种承诺。

## 启动、记录与异常

日常只双击 `start_booking.bat`：直接使用项目 `.venv`，不运行uv、不检查依赖指纹、不自动同步环境。首次安装或明确需要修复/更新依赖时，单独双击 `setup_environment.bat`，它才执行 `uv sync --locked`，完成后不会预约。不安装全局Python包。启动不再探测8080，抓包时才检查；再次启动仅替换本项目助手及捕获子进程，不关闭Clash或其他Python项目。

权限检查在助手进程内只执行一次，whoami/icacls各设置5秒等待超时，失败时不读取凭据并给出简短提示。启动阶段Ctrl+C不再输出Python堆栈；CMD自身仍可能问“终止批处理操作吗”，输入Y退出即可。截图中显示系统Python的标准库路径不代表此次又向全局安装了模块，虚拟环境本身也使用基础解释器的标准库。

**不要在等待时反复双击**：这会按你的单实例要求关闭旧助手。强制关闭时若已有提交意图，保守阻止再次提交，须先到小程序人工核对；不能为了启动顺畅自动抹去未知记录。

本地 `private/daily-settings.json` 保存每日偏好；`prepared-today.json` 保存最新快照，每轮另存唯一的 `prepared-*.json`，不会被手动再跑覆盖；`run-*.json` 保存结果、时序、首笔来源以及 `context.mode`（scheduled定时/immediate手动）、校时证据和准备文件名；`request-events.jsonl` 保存请求开始与耗时。整个private目录不提交Git。凭据只在本地保护目录保存。

历史失败说明：此前12:00:00.005触发后又执行约1556ms的场次GET，首笔POST因此拖到12:00:01.582附近。新路径移走这次GET；旧日志没有保留原业务失败原因，不能据此证明是售罄、配额或重复预约。新改动经过离线验证，真实成功率仍需你下一次正式运行的日志验证。

---


# 操作手册与全部输入字段

## 先分清五个概念

- **预约日期**：哪天打球；默认北京时间今天。不是脚本启动日期。
- **目标组**：一个日期、一个时段、候选场地和所需数量组成一组。
- **时段序号**：当次列表里的数字，一次只选一个；不是小时数。
- **候选场地**：允许程序尝试的范围。候选有五个而数量为一，只要成功一场就停止该组。
- **所需数量**：同一时段要实际预约几场，不是重试次数，也不是人数。

## 高级工具流程（主菜单8进入，以下数字仅指高级工具）

双击 start_booking.bat。Clash保留7897；凭据过期时先菜单1刷新，再菜单2验证。
菜单3只配置保存；菜单4立即执行，菜单5定时执行。执行前会再次展示目标，需要输入1确认；回车或0均返回。
只运行一份程序。再次双击会关闭本项目旧助手、恢复遗留代理再启动；旧订单不会被取消，提交结果未知仍须人工核对。

## 高级工具菜单：每项会做什么

| 数字 | 用途 | 是否下单/网络行为 |
|---|---|---|
| 1 | 刷新本人凭据；确认1后临时切代理到8080，经Clash7897转发；进入企业微信小程序场馆页面触发捕获 | 不下单；涉及代理，结束后恢复 |
| 2 | 验证本地凭据，并查询场馆类型确认服务端接受 | 一次只读请求；不下单 |
| 3 | 配置预约日期、场馆、目标组、时段、候选场地、所需数量 | 读取场次并保存配置；不下单 |
| 4 | 使用保存的配置立即执行 | 输入1确认才真实预约；不付款 |
| 5 | 使用保存的配置定时执行 | 转到每日快捷模式，使用每日设置，默认今天12点 |
| 6 | 发起三次类型查询，显示客户端RTT和粗略服务器Date信息 | 不下单；不要与正式运行并行 |
| 7 | 显示本地提交记录、结果和估计付款剩余时间 | 无订单查询；不能反映后续付款或取消 |
| 8 | 恢复强制关闭/断电前保存的系统代理 | 不下单；可能覆盖抓包期间其他代理改动 |
| 9 | 按配置观测场次20轮，每轮至少间隔3秒，写本地日志 | 不下单；不要与正式运行并行 |
| 10 | 查看本手册 | 无网络，不修改配置 |
| 0 | 退出当前菜单 | 不取消服务器订单 |

## 高级菜单3：逐字段解释

### 日期

- 输入1或回车：今天，终端会显示实际日期，每天变化。
- 输入2：再输入YYYY-MM-DD，例如2026-09-18。不能填写过去日期。
- 平台必须返回目标日期场次；程序不会猜测未开放日期的场次ID。

### 场馆

- 输入1或回车：仙林。
- 输入2：三牌楼。
- 一次向导只选择一个日期、一个校区；不要把此处的数字当场地号。

### 目标组数量

- 填1–6；回车默认1。表示接下来要分几轮填写需求，不是本次直接要几块场。
- 两个时间段各一场：填2，然后分别填写第1/2组、第2/2组。
- 同一时间段两场：填1，然后该组数量填2。
- 总需求上限6场。向导为后面每组预留至少一场，逐组显示可填数量上限；学校实际限制优先。

### 时段序号

程序每组重新列出真实返回的时段，例如：

```text
1（16:00–17:00）
2（17:00–18:00）
3（18:00–19:00）
4（19:00–20:00）
```

**一次只填一个数字。** 想选18:00–19:00，在这个示例里填3，不是填18，不是填18:00，也不能填3,4。
完成本组的场地和数量后，才会显示下一组的时段输入。序号以当次界面为准，不保证永远相同。

### 候选场地序号

- 这里允许多个数字，用半角或中文逗号分隔，如1,3,2或1，3，2。
- 填的是当前列表序号，不一定等于物理场号。必须看括号里的完整场地名和楼层。
- 只填一个候选，就不会自动换到名单外场地。
- 输入必须不重复、在范围内且非空；不支持1-8这种范围简写，也不支持“全部”。
- “当前可用”只是查询快照，不保证提交成功；“当前不可用”也可以列作候选，正式执行会重新查询。
- 候选顺序受下面的仙林分档优先级约束；同一档才按输入顺序。

### 场地优先级（按用户指定分类，不是学校官方单双打说明）

1. 仙林体育馆3楼1–8号（双打）
2. 仙林体育馆3楼9–12号（单打）
3. 仙林体育馆1楼任意（不区分单双打）
4. 仙林训练馆2楼任意（不区分单双打）
5. 其他或名称无法识别的场地（保留输入顺序）

支持3F/3楼/三楼等名称和中文场号。无法确认楼层或场号时不猜测。
只排序你选中的候选，不会自动添加更高优先级但你没有选的场地。三牌楼等其他场地保留原候选顺序。
实际引擎先匹配可完成的目标数量，避免候选少的组被其他组占掉唯一场地；存在组间竞争时，分配可行性可能让某组使用次优候选。分档规则不是保证每一笔都先提交3楼双打。
默认偏好不是强制只订双打；若你仅接受双打，就只选择3楼1–8号候选。

### 所需场数

回车默认1。例如选了1,3,2三个候选：数量1表示只成功其中一块，数量2表示需要两块不同场地。
最多不能超过已选候选数和剩余全局额度。数量不表示优先级、人数、小时数或重复抢同一块场地的次数。

### 保存与总订单上限

向导全部填完后校验，再原子保存到private/plan.json，替换上次配置。总订单上限自动等于各组数量之和，不另行输入。
中途Ctrl+C或校验失败不会保存新配置，上次成功保存的配置仍在。菜单4/5会先显示清单，请逐项核对，避免误执行旧配置。
不保证两组连续时段在同一物理场地，也不提供“必须全部抢到否则自动取消”；部分成功会保留。

## 两个时段各一场：完整填写示例

以下时段与场地序号仅为示例，须看你当前列表。

```text
菜单：3
日期：回车（今天）
场馆：回车（仙林）
组数：2
第1/2组时段：3（假设是18:00–19:00）
第1组候选：1,2,3（按当前列表确认完整场地名）
第1组数量：1
第2/2组时段：4（假设是19:00–20:00）
第2组候选：1,2,3（第二组列表可能不同，需重新核对）
第2组数量：1
保存清单：两个时段，各一场，总上限2
```

同一时间段两场：组数1→选一个时段→选至少两个候选→数量2。不要把同一时段建成两个重叠目标组；向导会拒绝这种冗余写法以防误操作；不是服务端拒绝原因的证据。
两个时段各两场：组数2→每组各选一个时段、至少两个候选、数量2→总上限4。是否允许四场仍取决于平台限制。

## 菜单4/5：执行字段

- 启动时间：脚本什么时候开始，不是打球时间。例如打球18:00–19:00，开抢时间可以是当天12:00:00。
- 格式YYYY-MM-DD HH:MM:SS，按北京时间。必须在未来一小时内。太早、已过时刻会拒绝，不会自动改成明天。
- 确认数字：1（真实预约，不付款）、0或回车（退出本次操作）。菜单1的1是开始捕获，不是预约。
- 等待期间保持电脑不休眠。凭据须覆盖启动时间后至少五分钟；时钟变化或错过窗口可能拒绝补跑。
- Ctrl+C中止不是取消订单。若请求已经发出，仍可能已经创建订单，请人工核对。

## 输出与异常含义

| 显示 | 含义/处理 |
|---|---|
| success / 接口返回成功 | 响应订单信息与目标匹配；立即到小程序核对并付款，不等全部目标结束 |
| complete | 本地执行已满足目标，不表示已经付款 |
| sold_out | 明确售罄，重新读取后可换名单内候选 |
| partial_or_exhausted | 部分成功或尝试/时间用尽；已成功订单不撤销 |
| blocked | 登录、额度、未支付订单、频率、重复时段规则或未知业务拒绝；记录安全分类/错误码/摘要后停止，不盲目重启 |
| unknown / submitting | 结果不明/程序在提交过程中退出；禁止重复提交，先核对订单 |
| HTTP 429 / Retry-After | 服务端限流/等待提示；停止并遵守等待时间，不能换账号或代理绕过 |
| 502 / 超时 | 网络或网关异常，不能证明请求到达学校；POST可能已经成功 |
| 历史成功记录阻止重复 | 不按五分钟自动清空；取消/过期需先人工确认，维护者登记后才能重订 |
| 8080占用 | 旧项目助手会被一键启动替换；未归属本项目的代理需手动停止 |

默认单请求在途、起始间隔至少一秒，最多20轮/60秒调度窗口；在途请求可能使退出更晚。每组最多max(3,数量)次提交，平台更严限制优先。
RTT是客户端完整响应耗时，不是纯服务端处理时间；Date通常是秒级，不能承诺毫秒级放场判断。只读观测日志记录采样变化，不证明精确放场瞬间。

## 数据与凭据

- private/session.json：本地凭据（不可发出/上传）。
- private/plan.json：当前目标配置（version、targets、max_orders；组字段id/date/start/end/courts/quantity）。不要用早期example.yaml执行正式预约。
- private/run-*.json：执行记录，可能含订单信息，只在本机核对。
- private/request-events.jsonl：脱敏请求分类、开始时间、耗时和状态。
- private/proxy-backup.json：待恢复的代理快照，不要在捕获运行时删除。

本机private目录虽限制访问权限，但不是加密保险箱。首次CA、登录和代理说明见SESSION_GUIDE.md。新默认值和优先级不自动扩大旧候选清单；想改变候选范围请重新菜单3配置。


## 定时失败的日志说明

`12:00:00.005` 表明定时启动本身成功；`slots GET 1555.71 ms` 表明关键延迟发生在放场后的场次读取；随后 `POST 133.94 ms / HTTP 200` 是服务端业务拒绝，不是网络超时。旧版本将无法识别的 `success:false` 都显示成 `unrecognized_business_failure`，新版保存安全分类、错误码、消息长度和摘要，不保存错误原文。

当时两个目标组完全重复。要同一时段两场，请一个目标组填数量2；不同时间段各一场才建两个组。完全重复组现在会在配置和执行前拒绝，这是输入防误操作检查，不是本次服务端拒绝原因的证据。


### 排查结论补充与修复边界

此次只发出一笔预约POST，两个组没有一起发送给服务器。因此不能推断“重复目标组”造成这次业务拒绝；一个组数量2与两组各数量1在客户端需求上均表示两场，建议合并只是避免填错。首次停止的直接原因是旧分类器收到success:false且未识别提示；旧日志未保留错误码及原文，无法还原学校具体拒绝原因，不能确认是售罄或限流。

12:01的记录有20次场次查询、没有任何POST，说明没有被引擎选中的可提交候选；旧版本没有每轮匹配数量，不能进一步证明所有场馆都满了。用户候选仅仙林3楼1–8号，优先级规则不会自动增加单打或其他楼层备选。

修复：定时前约20秒使用同一连接执行一次只读类型预热；距离目标不足20秒不插入预热。预热失败/休眠/时钟显著变化会停止，不提前提交、不跳过到点后的真实场次查询。private/latest-schedule.json记录目标时刻、预热结果及实际触发偏差。private/run-*.json增加每轮候选匹配、可用、未尝试可用数量。

预热针对日志里11:26最后访问后闲置约33分钟的风险；它可能减少重连开销，但不能保证消除服务端拥堵或1.56秒查询耗时。保持原请求频率，不采用并发绕限流。HTTP Date可能来自网关且只有秒级，与本机有偏差不能直接作为毫秒校时依据。

错误分类保守处理：“系统不可用”“请稍后”“预约失败”不再凭泛词认定售罄或限流；未知业务拒绝继续停止，仅明确售罄才尝试换场。历史原始错误没有保存，摘要也无法逆推出它；不能声称已经修复未知的服务端规则。

## 2026-09-18 定时失败复盘与修复

- 12:00自动轮首笔在本机12:00:00.000165进入POST，RTT30.42ms，返回业务失败7070，随后停止；20:00–21:00未尝试。12:01两笔成功属于用户手动触发的另一次运行，不能算自动轮成功。
- 12:05附近只读NTP样本+4.7165s、+4.7112s；12:10再次测得+4.7173s，HTTP Date粗略一致。证据表明本机慢约4.7秒，推断自动首笔在参考时间上可能已晚约4.7秒，不应只盯本机触发误差。测量发生在事后，不能证明业务服务器当时精确处理时间或7070含义。
- 修复3楼9–12号名称末尾“（单打）”导致漏选；原自动计划每时段只有21个候选，单打4场被漏掉。解析现在兼容中文/英文括号及空格，不把未知场馆加入候选。
- 签名时间戳与调度采用同一参考时钟，并在请求间隔等待之后生成，避免排队期间签名变旧。立即模式仍沿用本机时间，不强制NTP，和定时模式分别记录。
- 7070不能仅凭错误码映射成售罄。未知拒绝最多补一次只读场次快照，记录目标是否还存在、是否可用、ID是否一致；这个快照不是订单核验，不能据此重复下单。限流、认证和风控仍直接停止。
- 错误日志保留业务码、摘要、长度以及固定白名单业务词，不记录原始错误消息中的身份/凭据信息。旧消息只有摘要不能恢复，需小程序明确错误提示或官方说明才能补充7070的确切分类。
- 离线测试覆盖慢钟回放、校时失败关闭、单打候选、两个定时目标无中间GET、明确售罄刷新和未知错误只读后停止。下一次真实放场成功率尚未验证，不保证首选或两时段同场。

### 自动换场策略（当前生效）

- 不依赖7070的具体含义决定普通失败是否换场：必须是HTTP200且响应明确success:false，且没有任何订单生成迹象。已识别的登录、账户限额、未支付限制、风控、限流或业务规则禁止仍停止。
- 普通明确失败记录为rejected，售罄为sold_out；两者在定时模式都顺着预备候选继续，不重复同一场次，不等待额外场次GET。达到目标数量就停止该时段。
- 超时、断网、响应格式不明、返回成功但订单不匹配、失败响应携带订单信息都作为结果未知停止；缺少成功通知不等于服务器没有生成订单。
- 仍串行、请求开始至少间隔1秒、最多60轮、每组30次、提交阶段60秒（在途请求可能延长）；不是无限重试。当前每天两个时段各一场，成功后不自动取消、不继续抢替代场。
- 这项策略承认未知业务拒绝也可能是无法识别的全局原因；次数与时限约束用于限制此风险，不声称能够绕过平台限制。
