"""Inline field help shared by the console and documentation entry point."""
from pathlib import Path

GROUP_HELP = '''
【目标组怎么填】
一组 = 一个日期 + 一个时段 + 一份候选场地名单 + 所需场数。
两个时段各一场：组数填2；第1组和第2组各选一个时段，数量各填1。
同一时段两场：组数填1，选至少两个候选场地，数量填2。
时段序号一次只能填一个数字，例如1；不能填1,2，也不是输入18表示18点。
候选场地才支持1,3,2这样的逗号列表；候选多不等于全部预约。
两个组互相独立，允许部分成功，不保证连续时段是同一块场地。
'''

MENU = '''
南邮预约助手
1（刷新本人凭据：临时代理捕获；不预约）
2（验证凭据：访问一次只读接口；不预约）
3（配置目标：选择日期/时段/候选/数量；保存但不预约）
4（立即预约：使用已保存目标；需再次确认）
5（定时预约：指定执行时刻；需再次确认）
6（只读测速：3次请求；不预约）
7（本地结果：提交记录和估算支付期限；不是实时订单状态）
8（代理恢复：异常退出后恢复抓包前的系统代理）
9（只读观测：按配置观察20轮；不预约，勿与抢场同时运行）
10（完整帮助：查看所有字段、示例、限制与排障；无网络请求）
0（退出：不取消已有订单）
'''


def show_help():
    print((Path(__file__).resolve().parent/'USAGE.md').read_text(encoding='utf-8'))
